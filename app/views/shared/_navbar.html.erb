<nav class="bg-white px-6 relative shadow-md">
  <div class="flex flex-row justify-between items-center py-2">
    <h3 class="font-semibold text-3xl text-gray-500">Billing dashboard</h3>
    <div class="group flex flex-col items-center">
      <button class="p-2 rounded-lg md:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" class="h-10 fill-current" viewBox="0 0 16 16"><path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z" /></svg>
      </button>
      <div class="hidden group-hover:block md:block absolute md:static bg-white inset-x-0 top-16 py-3 shadow-md md:shadow-none text-gray-600">
        <div class="flex flex-row justify-center items-center text-center font-semibold text-gray-500">
          
          <% if Current.user %>
                        
            <%= link_to root_path, class: 'px-6 py-1 flex flex-col md:flex-row md:items-center' do %>
              <i class="fa-solid fa-house"></i> <span class="ml-2">Home</span>
            <% end %>

            <%= link_to references_path, class: 'px-6 py-1 flex flex-col md:flex-row md:items-center' do %>
              <i class="fa-solid fa-id-badge"></i> <span class="ml-2">Reference numbers</span>
            <% end %>

            <%= link_to everypay_index_path, class: 'px-6 py-1 flex flex-col md:flex-row md:items-center' do %>
              <i class="fa-solid fa-money-bill"></i> <span class="ml-2">Get data from Everypay</span>
            <% end %>

            <%= link_to white_codes_path, class: 'px-6 py-1 flex flex-col md:flex-row md:items-center' do %>
              <i class="fa-solid fa-users"></i> <span class="ml-2">White list</span>
            <% end %>

            <%= link_to logout_path, method: :destroy, data: { "turbo-method": 'delete' }, class: 'px-6 py-1 flex flex-col md:flex-row md:items-center' do %>
              <i class="fa-solid fa-arrow-right-from-bracket"></i> <span class="ml-2">Logout</span>
            <% end %>

          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
